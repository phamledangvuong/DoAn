<Window x:Class="phamledangvuong.ThietKeReport.Report"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:phamledangvuong.ThietKeReport"
        mc:Ignorable="d"
        Title="Report" Height="450" Width="800">
   
    <Grid Margin="20">

        <StackPanel>
            <TextBlock Text="DANH SÁCH SẢN PHẨM"
                   FontSize="20"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,10"/>
            <TextBlock Text="{Binding NgayNhap, StringFormat=Ngày: \{0:dd/MM/yyyy\}}" Width="761"/>
        </StackPanel>

        <DataGrid ItemsSource="{Binding SanPhams}"
              AutoGenerateColumns="False"
              Margin="0,60,0,40">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Tên sản phẩm"
                                Binding="{Binding TenSanPham}" />
                <DataGridTextColumn Header="Giá"
                                Binding="{Binding Gia}" />
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Text="© 2026 - Quản lý sản phẩm"
               HorizontalAlignment="Center"
               VerticalAlignment="Bottom"
               Margin="0,10"/>
    </Grid>
</Window>
